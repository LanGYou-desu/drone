# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# IDE
.idea/
.vscode/
*.swp

# Data (runtime — generated outputs, not tracked)
data/backup/
data/predict/
data/uploads/
data/fact/*.json
data/fact/self.dat

# OS
.DS_Store
Thumbs.db

# Config (real keys, not tracked)
config.json

# Reports (generated, not tracked)
reports/

# Models (deprecated paths — 模型现保存在 train/*/train_result/models/)
models/yolo/*.pt
models/yolo/*.onnx
models/yolo/*.engine
models/hybrid_predictor/*.pt
!models/yolo/README.md

# Ultralytics runs (deprecated — 训练输出已迁移到 train/*/train_result/)
runs/

# PDF
*.pdf
轨迹预测预测算法.txt
!docs/鹰眼长空使用手册.pdf
!docs/鹰眼长空开发文档.pdf

# Env
.env
# config.json is tracked (contains placeholder API key)
.playwright-mcp/

